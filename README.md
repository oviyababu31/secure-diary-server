```markdown
# Secure E-Diary — Server

This is a minimal Express server for the Secure E-Diary demo.

Run:
1. cd server
2. npm install
3. node index.js

By default it listens on port 4000 on all interfaces (0.0.0.0) so another machine on the same network can reach it.
```